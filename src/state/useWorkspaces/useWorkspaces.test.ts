import { renderHook } from '@testing-library/react-hooks';
import useWorkspaces from './useWorkspaces';

describe.skip('the useWorkspaces hook', () => {
  it.skip('should set isWorkspacesReady to true and export valid records when user is authed', () => {});
});
