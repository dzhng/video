import React from 'react';
import { screen, render, waitFor, within } from '@testing-library/react';
import fireEvent from '@testing-library/user-event';
import EditCall from './EditCall';

describe.skip('the EditCall component', () => {
  it('should render the correct values', () => {});

  it('should allow submission', () => {});

  it('should show error when required fields are not in place', () => {});

  it('should allow presentation uploads', () => {});

  it('should allow users to remove uploaded presentations', () => {});

  it('should allow users to remove existing presentations', () => {});
});
