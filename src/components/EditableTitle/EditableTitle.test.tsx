import React from 'react';
import { screen, render } from '@testing-library/react';

// TODO:
describe('EditableTitle', () => {
  it('should show loading skeleton when isLoading is set to true', () => {});

  it('should not call change callback when isLoading is set to true', () => {});

  it('should display input and submit when isLoading is set to false', () => {});

  it('should submit right away when debounceChange is set to false', () => {});

  it('should submit every 2 seconds by default', () => {});
});
